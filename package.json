{
  "homepage": "https://mosnes.github.io/delvers-bastionland-chargen/",
  "name": "delvers-bastionland-chargen",
  "version": "1.0.0",
  "description": "Character generator app for the Delvers setting of Electric Bastionland",
  "main": "server/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/server.js",
    "install": "cd",
    "build": "cd client && npm run build",
    "predeploy": "cd client && npm run build",
    "deploy": "gh-pages -d build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MosNes/delvers-bastionland-chargen.git"
  },
  "author": "MosNes",
  "license": "UNLICENSED",
  "bugs": {
    "url": "https://github.com/MosNes/delvers-bastionland-chargen/issues"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
